if %TEAM_ALPHA_SCORE != #NOTSET
if %TEAM_BETA_SCORE != #NOTSET

	setSize 34 * 1.333 * %VIDHEIGHT / %VIDWIDTH, 34
	setFontFamily "warsow"
	setCursor #WIDTH / 2, 0
	setAlign 3 1
	moveCursor -100 * 1.333 * %VIDHEIGHT / %VIDWIDTH, 0
	
	setColorToTeamColor #TEAM_ALPHA
	drawNum %TEAM_ALPHA_SCORE

	setCursor #WIDTH / 2, 0
	moveCursor 100 * 1.333 * %VIDHEIGHT / %VIDWIDTH, 0

	setAlign 1 1
		setColorToTeamColor #TEAM_BETA
		drawNum %TEAM_BETA_SCORE
	endif

endif
endif
